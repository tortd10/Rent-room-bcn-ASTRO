---
interface RoomDetailsProps {
  name: string;
  description: string;
  details: string;
}

const { name, description, details }: RoomDetailsProps = Astro.props;
---

<div class="flex flex-col justify-between">
  <!-- Nombre de la habitación -->
  <h1 class="text-3xl font-bold text-gray-900">{name}</h1>

  <!-- Descripción breve -->
  <p class="mt-4 text-gray-600">{description}</p>

  <!-- Detalles extensos -->
  <div class="mt-6">
    <p class="text-gray-700">{details}</p>
  </div>

  <!-- Botón de acción -->
  <div class="mt-6">
    <a 
      href="/contacto" 
      class="w-full bg-blue-600 text-white font-semibold py-3 px-4 rounded-lg text-center hover:bg-blue-700"
    >
      Reservar Ahora
    </a>
  </div>
</div>